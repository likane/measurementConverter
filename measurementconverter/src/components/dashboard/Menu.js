import React, { Fragment } from "react";
import { Link } from "react-router-dom";

const Menu = () => {
  return (
    <Fragment>
      <nav className="navbar navbar-dark bg-light">
        <span className="navbar-brand mb-0 h1">Converter</span>
      </nav>
    </Fragment>
  );
};

export default Menu;
